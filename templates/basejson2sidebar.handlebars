  <div class="wide-row blue-grey-background">
    <span class="w3-button row-left">Attributes in Component: {{"cim:IdentifiedObject.name"}}</span>
    <span class="w3-button row-right"
          onclick="showContainer('component-attributes', null);"><b>Close x</b></span>
  </div>
  <ul class="attribute-list light-background w3-ul">
  {{#each this}}
    <li class="attribute-item light-background">
      <p class="attribute-name light-background">{{@key}}</p>
      <p class="attribute-value light-background">{{this}}</p>
    </li>
  {{/each}}
  </ul>
