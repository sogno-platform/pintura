<ul class="attribute-list w3-ul">
{{#each this}}
  <li class="wide-row attribute-item dark-grey-background">
    <span class="row-left attribute-name">{{@key}}</span>
    <span class="row-right attribute-value">{{this}}</span>
  </li>
{{/each}}
</ul>

