<li class="wide-row floating-panel-item">
    <span class="row-left floating-panel-name">cim:Terminal</span>
    <span class="row-right wide-row  floating-panel-value">
        <input value="{{name}}" type="text"/>
            <button onclick="cimsvg.removeTerminal('{{type}}', '{{rdfid}}', '{{terminalId}}');cimsvg.populateTerminals('{{type}}', '{{rdfid}}')">  -  </button>
        <button onclick="populateAttributesIdOnly(cimsvg.getComponentAttributesNode(), '{{terminalId}}')"> -> </button>
    </span>
</li>
