{
  "name": "cimmenu-xslt",
  "version": "0.1.0",
  "license": "GPL-3.0-or-later",
  "scripts": {
    "generateClassStructure": "node generateClassStructure.js",
    "generateHandlebars": "node generateHandlebarTemplates.js --xmlDir=/data_model",
    "build": "npm run generateHandlebars && npm run generateClassStructure"
  },

  "devDependencies": {
    "glob": "^7.1.4",
    "handlebars": "^4.0.11",
    "libxmljs": "^0.18.7",
    "libxslt": "^0.7.0",
    "jszip": "^3.1.5"
  }
}
